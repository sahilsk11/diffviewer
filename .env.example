# Copy to .env or .env.local; both are gitignored.

# Optional absolute backend URL override. Leave empty for same-origin /api
# calls; Vite proxies /api to the local backend in development.
VITE_API_BASE_URL=

# Port the dev / preview server binds to (strictPort: true).
PORT=3000
