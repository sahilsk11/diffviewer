# Copy to .env or .env.local; both are gitignored.

# Absolute URL of the backend the bundle hits at runtime. Inlined into
# the production bundle at build time, also read in dev.
VITE_API_BASE_URL=http://localhost:8000

# Port the dev / preview server binds to (strictPort: true).
PORT=3000
